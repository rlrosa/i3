# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# default to tabbed
workspace_layout tabbed

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1: bash
bindsym $mod+2 workspace 2: rbox
bindsym $mod+3 workspace 3: www
bindsym $mod+4 workspace 4: vbox
bindsym $mod+5 workspace 5: 2nd
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10: rec

set $laptopScreen eDP1
set $secondMonitor "DP2-1"
set $thirdMonitor "DP2-2"

# move workspace
bindsym $mod+Control+Shift+l move workspace to output $secondMonitor
bindsym $mod+Control+Shift+u move workspace to output $thirdMonitor

# multi monitor setup
workspace "1: bash" output $laptopScreen
workspace "2: rbox" output $laptopScreen
workspace "3: www"  output $laptopScreen
workspace "4: vbox" output $secondMonitor
workspace "5: 2nd"  output $thirdMonitor
workspace 6  output $secondMonitor
workspace 7  output $thirdMonitor
workspace 8  output $thirdMonitor
workspace 9  output $thirdMonitor
workspace "10: rec" output $thirdMonitor

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace "1: bash"
bindsym $mod+Shift+2 move container to workspace "2: rbox"
bindsym $mod+Shift+3 move container to workspace "3: www"
bindsym $mod+Shift+4 move container to workspace "4: vbox"
bindsym $mod+Shift+5 move container to workspace "5: 2nd"
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace "10: rec"

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        output $secondMonitor
        tray_output none
}
bar {
        output $thirdMonitor
        tray_output none
}
bar {
       output $laptopScreen
       status_command i3status
       tray_output $laptopScreen
}

## rela

focus_follows_mouse no

# rhythmbox
#bindsym $mod+m exec rhythmbox
#bindsym $mod+Up exec rhythmbox-client --pause
#bindsym $mod+Down exec rhythmbox-client --play-pause
#bindsym $mod+Right exec rhythmbox-client --next
#bindsym $mod+Left exec rhythmbox-client --previous
# cmus
#bindsym $mod+Up exec cmus-remote --stop
#bindsym $mod+Down exec cmus-remote --pause
#bindsym $mod+Right exec cmus-remote --next
#bindsym $mod+Left exec cmus-remote --prev

# assign stuff to workspaces
assign [class="Rhythmbox"] 2: rbox
assign [class="Google-chrome"] 3: www
#assign [class="VirtualBox"] 4: vbox
assign [class="TeamViewer.exe"] 5: 2nd
assign [class="Wine"] 5: 2nd
assign [class="Qjackctl"] 10: rec
assign [class="Ardour"]   10: rec
# some floats
for_window [class="Yakuake"] floating enable
for_window [class="Nautilus" instance="file_progress"] floating enable
for_window [class="emulator-arm"] floating enable

# Start chrome
#bindsym $mod+g exec firefox
bindsym $mod+g exec google-chrome
# start pidgin
bindsym $mod+p exec pidgin

## default apps
exec --no-startup-id xterm -e ssh-add /home/$USER/.ssh/id_rsa
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id gnome-sound-applet
exec --no-startup-id dropbox start
#exec --no-startup-id davmail
#exec --no-startup-id megasync
exec --no-startup-id hipchat
#exec --no-startup-id rhythmbox
exec --no-startup-id yakuake
exec --no-startup-id workrave
exec --no-startup-id xcompmgr -c -n
# disable touchpad
exec --no-startup-id touchpad.sh --disable
# wall paper
exec_always --no-startup-id feh --bg-scale ~/Dropbox/i3/wallpaper.jpg
# setup multiple monitors
exec --no-startup-id mode_toggle.sh $laptopScreen $secondMonitor $thirdMonitor off
bindsym $mod+Shift+m exec monitor-toggle.sh $laptopScreen $secondMonitor $thirdMonitor
# toggle equalizer
bindsym $mod+Shift+n exec pulseaudio-equalizer toggle && notify-send "EQ toggled"
# enable transparency
exec --no-startup-id xcompmgr -n
## power saving (turn off monitor after 10m)
exec_always --no-startup-id xset dpms 600
# lock screen after 10m
set $i3lockrr "xset dpms force off && gnome-screensaver-command -l"
exec_always --no-startup-id gnome-screensaver
# disable locks, for video. reset i3 to re-enable
set $disableLock xautolock -disable && xset -dpms && notify-send "reload i3 to re-enable dpms"
bindsym $mod+XF86Launch1 exec $disableLock
bindsym $mod+Control+Shift+z exec $disableLock
# screen lock
bindsym $mod+Control+z exec $i3lockrr
bindsym XF86Launch1 exec $i3lockrr

#set $i3lockrr "i3lock -c 000000 -i ~/Dropbox/i3/bg-1680x1050.png -n -t"
#exec_always --no-startup-id xautolock -time 10 -locker '$i3lockrr -d' &
# disable locks, for video. reset i3 to re-enable
#bindsym $mod+XF86Launch1 exec xautolock -disable && xset -dpms && notify-send "reload i3 to re-enable dpms"

# brightness
#set $currentbrightness notify-send "Brightness: "`xbacklight -get |cut -f1 -d"."`"%" -i notification-display-brightness -t 100
set $bright_inc   "xbacklight -inc 3"
set $bright_dec "xbacklight -dec 3"
bindsym $mod+Control+Shift+minus exec $bright_dec
bindsym $mod+Control+Shift+plus  exec $bright_inc
bindsym $mod+F5 exec $bright_dec
bindsym $mod+F6 exec $bright_inc
bindsym XF86MonBrightnessDown exec $bright_dec
bindsym XF86MonBrightnessUp   exec $bright_inc

set $SINK 0
# volume - 16.04 + x220
bindsym XF86AudioLowerVolume exec pactl set-sink-mute $SINK 0 && pactl set-sink-volume $SINK -5% && killall -SIGUSR1 i3status && paplay /usr/share/sounds/ubuntu/stereo/message.ogg
bindsym XF86AudioRaiseVolume exec pactl set-sink-mute $SINK 0 && pactl set-sink-volume $SINK +5% && killall -SIGUSR1 i3status && paplay /usr/share/sounds/ubuntu/stereo/message.ogg
bindsym XF86AudioMute        exec pactl set-sink-mute @DEFAULT_SINK@ toggle && killall -SIGUSR1 i3status
bindsym XF86AudioMicMute exec amixer set Capture toggle && killall -SIGUSR1 i3status && paplay /usr/share/sounds/ubuntu/stereo/message.ogg
# volume - 16.04 + t450s
bindsym $mod+Control+minus exec pactl set-sink-mute $SINK 0 && pactl set-sink-volume $SINK -5% && killall -SIGUSR1 i3status && paplay /usr/share/sounds/ubuntu/stereo/message.ogg
bindsym $mod+Control+plus exec pactl set-sink-mute $SINK 0 && pactl set-sink-volume $SINK +5% && killall -SIGUSR1 i3status && paplay /usr/share/sounds/ubuntu/stereo/message.ogg
bindsym $mod+Control+0 exec pactl set-sink-mute $SINK toggle && killall -SIGUSR1 i3status
#bindsym $mod+Control+9 exec amixer set Capture toggle && killall -SIGUSR1 i3status && paplay /usr/share/sounds/ubuntu/stereo/message.ogg

# window borders with no border, toggle optional
new_window none
bindsym $mod+b border toggle
# calendar
bindsym $mod+c exec zenity --calendar
# screenshot
bindsym Print exec scrot 'screenshot-%Y-%m-%d_%H-%M-%S_$wx$h.png' -e 'mv $f /tmp/'
